# Core dependencies
PySide6>=6.6
opencv-python>=4.9
ultralytics>=8.3
paddleocr>=2.7
paddlepaddle>=2.6
pandas>=2.2
openpyxl>=3.1
numpy>=1.26
Pillow>=10.0

# Vision AI classification (optional — install the one you need)
# anthropic>=0.40
# openai>=1.50
# google-genai>=1.0

# CLIP embeddings for vehicle cache (optional — needed for AI vision)
# torch>=2.0
# transformers>=4.35

# Night/low-light enhancement
onnxruntime>=1.17

# TensorRT optimization (optional — install if NVIDIA GPU available)
# tensorrt>=8.6
